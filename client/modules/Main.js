var canvas = document.getElementById('canvas1');
Main.start(canvas);
var Main;
(function (Main) {
    function start(canvas) {
        initInput(canvas);
    }
    Main.start = start;
    function initInput(canvas) {
    }
    ;
})(Main || (Main = {}));
